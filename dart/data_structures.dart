void main() {
  //* You can directly add values to a list when you declare it
  //! Arraylists don't exist in Dart
  //* If you don't specify a type for the list, it will be dynamic
  List x = [1, "Yes", true];
  print(x);
}
